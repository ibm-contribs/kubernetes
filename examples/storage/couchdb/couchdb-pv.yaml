kind: PersistentVolume
apiVersion: v1
metadata:
  name: kubetest001-data-volume
  namespace: default
  labels:
    type: local
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Recycle
  hostPath:
    path: "/data1/data01"